// Only change code below this line
function randomRangeNumber(minNumber, maxNumber) {
    Math.floor(Mathrandom() *(max - min +1)) + min; 
    if(minNumber <= random) {
    return random;
    }
    if(maxNumber >= random) {
        return random;
    }
}
    randomRangeNumber(20, 5);
    console.log(randomRangeNumber(20, 5));
 // Only change code above this number

    module.exports = randomRangeNumber;